import { styled } from '@mui/material';

export const StyledHomeWrapper = styled('main')(({ theme }) => ({
  width: '100%',
  height: '100%',
  minHeight: '100vh',
  background: 'black',
  // [theme.breakpoints.down('md')]: {

  // },
}));
